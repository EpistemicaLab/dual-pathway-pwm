# Core scientific stack
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

# JAX for differentiable simulators + encoder training (CPU-only OK for many cells;
# GPU recommended for §5.4 calibration-fair sweep and §5.8 12-method alignment grid).
jax>=0.4.20
jaxlib>=0.4.20

# Plotting (for figure regeneration)
matplotlib>=3.7

# Optional: GPU JAX. To install:
#   pip install --upgrade "jax[cuda12]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
